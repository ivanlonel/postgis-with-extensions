version: "3.7"

services:
    postgres:
        build:
            context: .
            args:
                - BASE_IMAGE_TAG=$DOCKER_TAG
        environment:
            - POSTGRES_PASSWORD=postgres
        networks:
            - pgtest_network
        ports:
            - "5432:5432"
    
    sut:
        build: ./tests
        depends_on:
            - postgres
        networks:
            - pgtest_network

networks:
    pgtest_network:
        driver: bridge